"""CLI command modules. Each exposes a ``register(sub)`` function."""

#: Help text for the ``--json`` flag every parser in this package adds. One
#: constant so the wording stays identical across every verb and noun.
JSON_FLAG_HELP = "Emit structured JSON."
